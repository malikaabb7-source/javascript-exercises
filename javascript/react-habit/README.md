# React Habit Tracker

A simple daily habit dashboard built with React and Vite, focusing on `useState` and immutable updates.

## Features
- Add daily habits with custom titles
- Toggle completed status for today with auto streak counter
- Remove habits by unique ID
- Live summary progress banner

## How to Run
1. Clone the repository
2. Run `npm install`
3. Run `npm run dev`
